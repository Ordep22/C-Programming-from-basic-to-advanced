#include <stdio.h>
#include <string.h>


int main(){

        char string[] = "Hello Word";
        int len = 0;

        len = strlen(string);
        printf("The size of the string is: %d",len);




}
